service: yara-villa-api
useDotenv: true

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  environment:
    EMAIL_USER: ${env:EMAIL_USER}
    EMAIL_PASS: ${env:EMAIL_PASS}


functions:
  api:
    handler: server.handler
    events:
      - httpApi: '*'
